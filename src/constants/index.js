import * as actionTypes from './action-types'
import * as apiUrls from './api-urls'
import * as uiSettings from './ui'

export const urls = apiUrls
export const types = actionTypes
export const ui = uiSettings
